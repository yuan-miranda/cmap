## Coordinate Mapper (cmap)

Simple player coordinate logger in Minecraft. It then uses a python script [here](https://github.com/yuan-miranda/gencmap) to generate an image map the coordinates. The coordinates are stored in `player_coordinates.txt`. Its located either in `cmap/run` or where this mod's `.jar` file is located.
